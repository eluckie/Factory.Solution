@{
  Layout = "_Layout";
}

@using Factory.Models;
<div id="index">
  <div id="details">
    <h2><span id="ename">Meet the family</span></h2>

    @if (Model.Count == 0)
    {
      <p id="accent">Yea, nobody's home 😔</p>
    }
    @foreach (Engineer dude in Model)
    {
      <p><span id="list">@Html.ActionLink(@dude.Name, "Details", new { id = @dude.EngineerId })</span> - @dude.Pronouns</p>
    }
  </div>
  <hr>
  <br>

  <p id="elink">@Html.ActionLink("Add a new engineer", "Create", "Engineers")</p>
  <p id="mlink">@Html.ActionLink("Add a new machine", "Create", "Machines")</p>
  <p id="mlink">@Html.ActionLink("Manage machines", "Index", "Machines")</p>
  <p id="hlink">@Html.ActionLink("All the way nvm", "Index", "Home")</p>
</div>